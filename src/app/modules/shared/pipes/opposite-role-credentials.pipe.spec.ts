import { OppositeRoleCredentialsPipe } from './opposite-role-credentials.pipe';

describe('OppositeRoleCredentialsPipe', () => {
  it('create an instance', () => {
    const pipe = new OppositeRoleCredentialsPipe();
    expect(pipe).toBeTruthy();
  });
});
