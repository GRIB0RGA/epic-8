import { GetOcupationPipe } from './get-ocupation.pipe';

describe('GetOcupationPipe', () => {
  it('create an instance', () => {
    const pipe = new GetOcupationPipe();
    expect(pipe).toBeTruthy();
  });
});
